<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.testeagenda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_savio_apk_novo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_savio_apk_novo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TesteAgenda">
        <activity android:name=".FuncionalidadeNovaActivity"></activity>
        <activity android:name=".TestandoActivity" />
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityClientesVip">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".ListaActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity android:name=".AgendaActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
    </application>

</manifest>